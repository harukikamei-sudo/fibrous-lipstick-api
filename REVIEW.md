# レビュー指示

このファイルはレビュー専用のガイダンスである。`.github/workflows/claude-code-review.yml` から読まれる。

`CLAUDE.md`（プロジェクト全体の指示）とは役割が違う。**ここに書くのはレビューの振る舞いを変える指示だけ**で、一般的なプロジェクトの説明は `CLAUDE.md` に残す。長くすると重要なルールが薄まる。

---

## 前提 — 仕様は渡されていない

**このレビューには受け入れ条件が渡されていない。** PR のタイトル・本文・ブランチ名も参照できない。渡された `diff` と、リポジトリのコードだけで判断する。

これは意図的である。仕様を持つレビュー（Review②）とは別の集合を見るために、入力を変えている。

したがって次を報告しない。

- **「仕様に合っているか」の判断。** 判断材料が無い
- **「この変更は必要か」の問い。** 意図を知らされていない
- 設計方針への異議。**壊れているかどうかだけを見る**

## 探すもの

- 到達不能な分岐、条件式の誤り、境界の取り違え
- 例外・`None`・空・型の扱い漏れ
- **呼び出し元を壊す変更**（`diff` の外を読んで確かめる）
- 既存の規約から外れた書き方
- 追加された処理に対応するテストが無いもの

## 報告しない

- CI が既に実行しているもの（lint、フォーマット、型検査）
- 生成物、ロックファイル、ベンダーされた依存
- スタイル・命名・リファクタリングの提案
- **推測だけの指摘。** 動作に関する主張は `ファイル:行` を挙げる

## このリポジトリで常に見るもの

- **`_validate_url` / `_fetch_image` を変える差分**は、SSRF の防御が緩まないかを見る。`http(s)` 以外・loopback・private・link-local・multicast・reserved の拒否が残っているか
- **`km.py` / `estimate_s.py` の数値**を変える差分は、`t_light = 0.3` の規約を崩していないか。K-M は観測に `S·t` しか効かないため、`t` を動かすと過去に出した `S` と比較できなくなる
- テストは `pytest` ではなく**スクリプトとして直接実行する形式**。`test_*.py` に `pytest` 前提の書き方（`fixture`、`parametrize`）が混ざっていないか
- `assets/lips/*.png` など**追跡しない方針のバイナリ**が追加されていないか

## 出力の形

```
findings:
- 場所: <ファイル:行>
  種別: 型 / 境界条件 / 回帰 / 並行性 / 性能 / その他
  説明: <何がどう壊れるか。1〜2行>
  確度: high | low
```

- **上限5件。** 超えたら「ほかに N 件」とだけ書く
- `確度: low` は「気になるが確信がない」もの
- 何も無ければ `findings: (なし)` だけ返す

## マージを止めない

**このレビューは非拘束である。** 結果は参考として残すだけで、承認もブロックもしない。

止める力を持たせると、誤検出のたびに人の手が止まり、使われなくなる。

## 修正しない

指摘だけを返す。**コードを書き換えない。テストも実行しない**（テストは CI の担当）。
