__pycache__/
*.pyc
.venv/
images_cache/
thumbnails/
*.csv
!products.csv
!products_with_lab.csv
*.xlsx
.DS_Store

# ダウンロード画像が紛れ込まないよう webp はリポジトリ全体で追跡しない
*.webp

# ログ・スナップショット
*.log
*.csv.phase1plus
before_lab.json
summary*.md
unzipped/
# UI 用画像はバイナリサイズが HF Spaces のポリシーに抵触しがちなので
# 原則 git 追跡しない(ローカルで配置 or ダミー生成で動作)。
# 必要なら HF Hub Xet ストレージ経由で別管理する(README 参照)。
assets/lips/*.png
assets/lips/*.jpg
assets/lips/*.jpeg
assets/lips/*.webp

# 試着デモは独立した HF Space リポジトリ(hf_streamlit_space/.git)。
# 親リポでは追跡せず、URL と手順を CLAUDE.md / HANDOFF.md に記録する。
hf_streamlit_space/

# personas_cli plot の生成物(再生成可能なので追跡しない)
bayes_report.png
bayes_convergence.png
bayes_eig.png
.vercel
.env*.local
